;;; markup.el --- Provide features for markup modes -*- lexical-binding: t; -*-

;;; Commentary:

;;; Code:

(use-package json-mode)

(use-package yaml-mode)


(provide 'pecus-markup)

;;; markup.el ends here
